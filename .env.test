# define your env variables for the test env here
KERNEL_CLASS='App\\Kernel'
APP_SECRET='$ecretf0rt3st'

# --- Application Feature Flags ---
LEADS_FEATURE_ENABLED=false

# --- Application Config ---
# Company and outreach details used in emails/footers
OUTREACH_COMPANY_NAME="CleanWhiskers"
OUTREACH_COMPANY_ADDRESS="123 Business Rd, City, Country"
OUTREACH_CONTACT_EMAIL="support@example.com"

# Safety controls
LEAD_CLAIM_TTL_MINUTES=60

# Base URL of the app (used in links)
APP_BASE_URL="http://localhost"

# Mirror defaults used by CORS bundle to avoid EnvNotFound
CORS_ALLOW_ORIGIN='^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$'

# Disable CAPTCHA in tests
CAPTCHA_ENABLED=false
CAPTCHA_PROVIDER=none

# Optional: explicit empty site keys to avoid resolution issues
RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET=
HCAPTCHA_SITE_KEY=
HCAPTCHA_SECRET=
