{% extends 'base.html.twig' %}

{% block title %}Lead Claimed{% endblock %}

{% block body %}
  <section class="container container--narrow mt-8 mb-8">
    <header class="mb-6">
      <h1>Lead claimed successfully</h1>
      <p>You claimed the {{ serviceName }} lead in {{ cityName }}.</p>
    </header>

    <div class="card mb-6">
      <p class="mb-4"><strong>Service:</strong> {{ serviceName }}</p>
      <p class="mb-4"><strong>City:</strong> {{ cityName }}</p>
      {% if lead.claimedAt %}
        <p class="mb-0"><strong>Claimed at:</strong> {{ lead.claimedAt|date('H:i, M j') }}</p>
      {% endif %}
    </div>

    <div class="card card--info mb-6">
      <p class="mb-4"><strong>Free leads for the next 2 months</strong></p>
      <p class="mb-0">
        We’ll send new grooming leads to your inbox for free over the next two months. After that, leads will be paid.
        If you’re fine with this, no action is needed — just watch your inbox. You can unsubscribe anytime from any email we send.
      </p>
    </div>

    {% if undoEnabled and releaseUrl and releaseUntil %}
      <div class="card card--warning mb-6">
        <div class="flex items-center flex-wrap" style="gap:1rem; justify-content:space-between;">
          <div style="min-width:220px; flex:1;">
            <p class="mb-4"><strong>Changed your mind?</strong></p>
            <p class="mb-0">You can release this lead until {{ releaseUntil|date('H:i, M j') }}.</p>
          </div>
          <a class="btn btn--accent tap-target" href="{{ releaseUrl }}" aria-label="Release this lead">Release this lead</a>
        </div>
      </div>
    {% endif %}

    <h2 class="mb-4">Owner contact</h2>
    <div class="card mb-6">
      <p class="mb-4"><strong>Name:</strong> {{ lead.fullName }}</p>
      {# Temporarily hide email on success page #}
      {# <p style="margin:0 0 .25rem 0;"><strong>Email:</strong> <a href="mailto:{{ lead.email }}">{{ lead.email }}</a></p> #}
      {% if lead.phone %}
        <p class="mb-4"><strong>Phone:</strong> <a href="tel:{{ lead.phone }}">{{ lead.phone }}</a></p>
      {% endif %}
      {% if lead.petType %}
        <p class="mb-4"><strong>Pet type:</strong> {{ lead.petType }}</p>
      {% endif %}
      {% if lead.breedSize %}
        <p class="mb-0"><strong>Care notes:</strong> {{ lead.breedSize }}</p>
      {% endif %}
    </div>

    <h2 class="mb-4">Next steps</h2>
    <ol style="padding-left:1.25rem; margin:0;">
      <li>Introduce yourself by phone within 1–2 hours.</li>
      <li>Reference their request for {{ serviceName }} in {{ cityName }}.</li>
      <li>Share availability, pricing, and how to book.</li>
      <li>If no response, follow up politely within 24–48 hours.</li>
    </ol>

  </section>
{% endblock %}
