{% extends 'base.html.twig' %}

{% block title %}Thank you | CleanWhiskers{% endblock %}

{% block body %}
  <section class="container container--narrow mt-8 mb-8" aria-labelledby="thank-you-title">
    <header class="mb-6">
      <h1 id="thank-you-title">Thanks — your request is in!</h1>
      <p>We’ve alerted groomers in {{ city.name }} about your {{ service.name|lower }} request. You’ll hear back shortly with availability and pricing.</p>
    </header>

    <div class="card mb-6">
      <h2 class="mb-4">What happens next</h2>
      <ol style="padding-left: 1.25rem; margin: 0;">
        <li>Local pros review your request details.</li>
        <li>You receive replies with timing and quotes.</li>
        <li>Pick a groomer and confirm your booking.</li>
      </ol>
    </div>

    <div class="flex items-center flex-wrap" style="gap: .75rem;">
      <a class="btn btn--accent tap-target" href="/groomers/{{ city.slug }}/{{ service.slug }}">Browse groomers in {{ city.name }}</a>
      <a class="btn tap-target" href="/">Back to Home</a>
    </div>
  </section>
{% endblock %}
