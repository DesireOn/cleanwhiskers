<section class="get-started reveal-on-scroll" aria-labelledby="get-started-heading">
    <div class="get-started__inner container">
        <h2 id="get-started-heading" class="get-started__title">{{ 'Concierge mobile grooming, simplified'|trans }}</h2>
        <p class="get-started__subtitle">{{ 'Choose how CleanWhiskers can support your next appointment.'|trans }}</p>

        <div class="get-started__options" role="list">
            <article class="get-started__card" role="listitem">
                <div class="get-started__icon" aria-hidden="true">
                    <img src="{{ asset('illustrations/search.svg') }}" alt="" loading="lazy">
                </div>
                <h3 class="get-started__heading">{{ 'Get a concierge match'|trans }}</h3>
                <p class="get-started__text">{{ 'Share your pet\'s needs and we\'ll introduce the right mobile groomer.'|trans }}</p>
                <a href="#search-form" class="get-started__action btn btn--accent tap-target">{{ 'Start your quote'|trans }}</a>
            </article>

            <article class="get-started__card" role="listitem">
                <div class="get-started__icon" aria-hidden="true">
                    <img src="{{ asset('illustrations/paw.svg') }}" alt="" loading="lazy">
                </div>
                <h3 class="get-started__heading">{{ 'Join the CleanWhiskers network'|trans }}</h3>
                <p class="get-started__text">{{ 'Apply to receive concierge-managed leads from engaged pet parents.'|trans }}</p>
                <a href="/register?role=groomer" class="get-started__action btn btn--accent tap-target is-disabled" aria-disabled="true" tabindex="-1" title="{{ 'Coming soon'|trans }}">{{ 'Join the waitlist'|trans }} <span class="badge">{{ 'Coming soon'|trans }}</span></a>
            </article>
        </div>
    </div>
</section>
